# .github/workflows/environment-approval.yml
name: Environment Approval Test

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production  # This references your environment
    steps:
      - name: Before Approval
        run: echo "This step runs immediately"
      
      - name: Deploy (After Approval)
        run: |
          echo "🎉 Approved! Deploying now..."
          echo "✅ This only runs after manual approval"
